import React from 'react'

const Test = (props) => {
  return (
    <div>
        <center>
            <h1>name:{props.name} and age;{props.age}</h1>
        </center>
      
    </div>
  )
}

export default Test
